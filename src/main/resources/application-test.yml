spring:
  config:
    activate:
      on-profile: test

  datasource:
    url: jdbc:h2:mem:eventitta-test;DB_CLOSE_DELAY=-1;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  h2:
    console:
      enabled: true
      path: /h2-console

jwt:
  secret: eb583f5872f5bde4833167f91002f263fdec66cc9b3be80eb3517692dd8c245b
  access-token-validity: 1800  # 초 (30분)
  refresh-token-validity: 1209600  # 초 (14일)

logging:
  level:
    root: DEBUG
    org.hibernate.SQL: DEBUG

